import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.princeiyke.inventory',
  appName: 'Prince Iyke Inventory',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
